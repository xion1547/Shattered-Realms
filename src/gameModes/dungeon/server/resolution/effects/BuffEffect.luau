-- Effect handler: BUFF. apply(target, effect, source).

local BuffEffect = {}

function BuffEffect.apply(target, effect, source)
    -- TODO: BuffService:apply(target, BuffDefinitions[effect.buff], source)
end

return BuffEffect
